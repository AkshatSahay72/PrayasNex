# Frontend Environment Variables Template
# When deployed on Vercel, set VITE_API_URL to your Render backend domain (e.g. https://prayasnex-backend.onrender.com/api)
# In local development, leave empty to use Vite's built-in proxy (/api -> http://127.0.0.1:8000)
VITE_API_URL=
